{
  "name": "museovirasto-pmtiles-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "npm run build && wrangler dev --local",
    "measure:browser": "node scripts/measure-browser.mjs",
    "seed": "bash ../scripts/12-seed-local-r2-poc.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.worker.json --noEmit && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ol": "10.10.0",
    "ol-pmtiles": "2.0.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.0.0",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "4.1.11",
    "wrangler": "4.125.0"
  }
}
